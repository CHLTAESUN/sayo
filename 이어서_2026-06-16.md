# SAYO 이어서 하기 — 2026-06-16 세션 인수인계

> 컨텍스트 비우고 이어받는 문서. 먼저 이 파일 → HANDOFF.md → CLAUDE.md → SECURITY.md 순으로 읽기.
> 작업 폴더: `C:\Users\SBS\Documents\Codex\2026-06-11\new-chat\work\onmaeul`
> repo: github.com/CHLTAESUN/sayo · 라이브: https://chltaesun.github.io/sayo/ · Supabase ref: swhkdzuhsjcticzoteen
> 최신 커밋: e675ac8 (전부 push·발행됨)

## 이번 세션에 완료한 것 (전부 라이브 발행됨)

### 1. 가입을 아이디 기반으로 전환
- 로그인 신원 = 항상 `아이디@sayo.app` (가짜 이메일). 실제 이메일은 **선택 입력**(프로필 contact_email로 보관).
- 로그인도 아이디 입력 시 `@sayo.app`로 매핑. (main.jsx completeSignup / loginLocalAccount)

### 2. 이메일 인증번호 (작동 중)
- 발송: **Brevo**(무료, 하루 300통). 발신자=xotjs93235692@gmail.com(인증됨).
- 백엔드: Supabase Edge Function **email-verify** 배포됨(JWT verify OFF). 저장 테이블 **email_codes**(RLS, service_role만).
- 시크릿(Supabase Edge Functions Secrets): `BREVO_API_KEY`, `BREVO_SENDER`.
- 프론트: src/lib/emailVerify.js. 가입 화면 이메일칸에 "인증번호 발급/확인" 버튼. 지메일·네이버 발송·확인 테스트 통과.

### 3. 봇 차단 캡차 — Cloudflare Turnstile (작동 중)
- 가짜 "로컬 테스트 본인확인" 제거 → Turnstile 위젯으로 교체. 가입·로그인 양쪽에 적용.
- Cloudflare 위젯 'sayo' (관리형, 호스트: chltaesun.github.io + localhost).
- **Site Key (공개): `0x4AAAAAADlqsEfCIjTZni5A`** → `.env`(VITE_TURNSTILE_SITE_KEY) + GitHub Actions 시크릿 + deploy.yml env.
- **Secret Key (비밀)** → Supabase Authentication → Attack Protection → CAPTCHA(Turnstile)에만 저장. (코드/repo에 없음)
- index.html CSP에 `challenges.cloudflare.com` 허용(script/frame/connect-src).
- src/Turnstile.jsx. 키 없으면 자동 비활성. 로컬·라이브 둘 다 위젯 통과 + 서버검증까지 실제 가입 테스트 통과.

### 4. Supabase 설정 변경(대시보드)
- Authentication → **Confirm email OFF**, **Allow new users to sign up ON**.
- Attack Protection → **CAPTCHA 보호 ON / Turnstile / Secret Key 입력됨**.

### 5. UI 다듬기
- 가입에서 **공개범위·메시지수신 select 제거**(가입 후 설정에서). 약관 3개 미동의 시 가입 차단(안전장치).
- **인기 게시물**: 글씨 크기 조정(제목 12.5/부가 11/통계 10px), **메시지 섹션과 동일 크기**. 좁은 화면(≤1250px)에선 피드 안 `.feed-popular` 블록으로도 표시.
- **인기 게시물 클릭 → 게시물 상세 팝업(PostDetail)**: 본문·별·답글·재게시·댓글. 별/답글/저장 동작. (popularPosts 데이터에 body/handle/comments 추가함)
- **새 글 쓰기** → 글쓰기 팝업(모달, 큰 입력칸+게시하기). 어느 탭에서든 열림. 게시 실패 시 안내, 비로그인 시 로그인창.
- **프로필 편집** → 로그인 시 설정 모달, 게스트는 로그인창.

## ⏸ 진행 중이던 것 (여기서 멈춤)
- 사용자가 **로그인 계정**을 원함. **이 앱엔 '관리자 계정' 개념 없음 — 전부 일반 계정.**
- 계정 만들기는 **협업 필요**: 보안 규칙상 에이전트는 비밀번호 입력·계정 생성·캡차 클릭 불가.
  → 가입 시 **사용자가 직접**: ① 캡차 체크박스 클릭 ② 비밀번호 입력. 나머지(이메일 skip·아이디·이름·생일·약관)는 에이전트가 채움.
- 멈춘 지점: 회원가입 화면 1단계(캡차 클릭 대기). 아이디 미정(기본 제안 `admin`).
- 사용자가 보낸 "**1**" = 아마 아래 '팔로워 목록 만들기(A/B)' 질문의 답(A=만들기). 다음에 확인 필요.

## 다음에 할 후보 (열린 작업)
- **로그인 계정 만들기 마무리** (위 진행 중) → 그 계정으로 글쓰기/팔로우 등 실제 동작 확인.
- **아직 동작 안 하는 것들** (사용자가 "버튼 안 됨" 지적):
  - 팔로워/팔로잉 숫자 → 누르면 목록 보이게? **팔로워 목록 화면 없음**(데이터도 없음). 새 기능 필요. (사용자 "1"=만들기일 가능성)
  - 프로필 탭 게시물/답글/별 → 전환 동작 없음(답글/별 데이터 없음).
  - 둘러보기 '추천하는 사람'의 팔로우 버튼 등 일부 dead.
- **사진 업로드 미연결**(Supabase Storage). 이미지 영속 안 됨.
- **답글/팔로우/메시지 DB 연결**, 프로필 헤더 실명화(현재 프로필은 목업 me=김지우).
- auth-gate(한국 IP 검문) Edge Function 미배포(프론트는 fallback로 정상 동작).

## 알려진 사소한 이슈
- 새로고침 직후 **getSession 타이밍** 때문에 게스트가 '둘러보기'를 너무 빨리 누르면 로그인 모달이 잠깐 다시 뜰 수 있음(세션 확인 끝나면 안정). 필요시 useEffect 정리.
- 게시물 이미지/대부분의 쓰기 동작은 **로그인 필요**(게스트는 구경만).

## 명령
```
cd "C:\Users\SBS\Documents\Codex\2026-06-11\new-chat\work\onmaeul"
git pull
npm install   # 처음만
npm run dev    # http://localhost:5173
# 수정 후: git add -A && git commit -m "..." && git push  (= 라이브 발행)
```
키는 .env(gitignore)에만. service/secret 키는 repo·vault·채팅에 저장 금지.
